[pytest]
testpaths = tests
# Disable a globally-installed web3 plugin that crashes collection on this
# machine (unrelated eth_typing version clash). Harmless if absent.
addopts = -p no:pytest_ethereum
