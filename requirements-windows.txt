# Windows-native deps — DEFAULT XGB path, no PyTorch (its Windows wheel has a
# known c10.dll load failure). Everything below runs natively on Windows:
# training, GEE export, raster alignment, plots, folium maps, dashboard.
# For the optional PINN model use WSL (see README).

# --- geospatial + GEE ---
earthengine-api>=0.1.390
geemap>=0.32
rasterio>=1.3
rioxarray>=0.15
xarray>=2024.1
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
osmnx>=1.9

# --- meteorology ---
cdsapi>=0.6
netCDF4>=1.6

# --- ML (no torch) ---
numpy>=1.26,<2.3
pandas>=2.1
scikit-learn>=1.4
xgboost>=2.0          # physics-informed via monotone_constraints
shap>=0.44

# --- viz / dashboard ---
folium>=0.15
streamlit-folium>=0.20    # interactive region picker in the dashboard
matplotlib>=3.8
plotly>=5.18
streamlit>=1.31

# --- utils ---
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.66
joblib>=1.3
